import Typography from "typography"
import fairygate from "typography-theme-fairy-gates"

const typography = new Typography(fairygate)

export const { scale, rythm, options } = typography
export default typography
